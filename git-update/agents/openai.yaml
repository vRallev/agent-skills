interface:
  display_name: "Git Update"
  short_description: "Fetch and prune remote Git refs safely"
  default_prompt: "Use $git-update to run git fetch --prune in this repository without changing local branches."
