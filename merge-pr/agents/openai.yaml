interface:
  display_name: "Merge PR"
  short_description: "Merge approved pull requests through CI and queues"
  default_prompt: "Use $merge-pr to merge the pull request from context or a supplied PR number, and keep working until GitHub confirms it is fully merged."
