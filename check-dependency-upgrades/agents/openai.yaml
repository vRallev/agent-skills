interface:
  display_name: "Check dependency upgrades"
  short_description: "Audit catalogs and Gradle wrapper upgrades"
  default_prompt: "Use $check-dependency-upgrades to audit Gradle catalogs and wrapper versions, separating ready upgrades from intentional holds."
